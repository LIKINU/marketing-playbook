# 系统与编辑器
.DS_Store
Thumbs.db
*.swp
*~

# 项目内部记忆（不进仓库）
.workbuddy/

# 备份与临时文件
*.zip
*.bak

# 实测/测试产物（本地测试用，不进仓库）
实测-*/
__pycache__/
*.pyc

# 长内容解析产物（本地工作稿：把书/长文拆成一节节的中间产物，不进仓库）
#   ⚠️ 2026-09-19 加：`解析产物/王冲108种商业模式-全量解析.md`（118 KB）一直没入库、
#      也零入链，却让 `repo_hygiene.py` 每轮都报「孤儿档」——**噪声会把真问题埋掉**。
#      这份是**本地工作稿**，不是仓库资产 → 收进这里（不删文件，只是不让它进仓库）。
解析产物/

# ── 内部件：与「营销方案生成器」这个 skill 的功能无关，不进公开仓库 ──────────
#   2026-09-21 用户裁定：「跟 skills 無關的都不要在裏面」。
#   这些文件**本地保留**（部分还挂在本地 hook / 回归流程上），只是不再随仓库分发。
                     # 内部改进过程记录：派 agent 审查、问题清单、修复排期
                # 写给 AI 的仓库快照（省上下文用），不是 skill 资产
scripts/        # 上面那份的生成器
scripts/   # 把 skill 镜像到个人 笔记库 笔记库 —— 私有运维，含私有路径
